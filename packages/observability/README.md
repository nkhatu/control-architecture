# Observability

This package should standardize:

- trace names and span attributes
- metric names and rail tags
- structured log fields
- audit event fields
- correlation ID propagation

The first dashboards should answer:

- where the workflow is paused
- which policy rule blocked or escalated the task
- whether any release attempt produced an ambiguous downstream response
