# Helm

Use this directory for environment packaging when services are deployable.

Chart goals:

- one chart per service
- shared chart helpers for tracing, secrets, and service identity
- environment overlays for local, dev, and pre-prod
