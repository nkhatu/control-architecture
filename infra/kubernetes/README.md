# Kubernetes

Use this directory when the PoC moves beyond local Docker-based infrastructure.

Expected manifests:

- per-service deployments
- network policies
- config maps for registries and control-plane config
- secret references
- pod identity or workload identity bindings
